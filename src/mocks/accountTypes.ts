import type { AccountTypeRecord } from "@/lib/data";

const mockAccountTypes: AccountTypeRecord[] = [
  { id: "std", account_type_name: "Standard" },
  { id: "prm", account_type_name: "Primary" },
  { id: "prp", account_type_name: "Primary+" },
];

export default mockAccountTypes;
